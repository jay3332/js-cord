/**
* Automatically paginates a large block of text.
* Embeds, prefixes, and suffixes may be supplied.
*/

const Paginator = require("./Paginator");

class TextPaginator extends Paginator {
    constructor(messageable, text, { prefix="", suffix="", maxSize=2000, wrapAt=[" ", "\n"], embed, emojis, raw, restricted, user }={}) {
        
    }
}
